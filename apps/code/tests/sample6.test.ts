export const solution6 = (n: number) => {
    let result = 4

    return result
}

describe("solution6", () => {
    it("should work", () => {
        const input = 3

        const output = solution6(input)

        const expected = 4

        expect(output).toEqual(expected)
    })
})
